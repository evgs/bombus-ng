#pragma once

#include <string>

namespace std {
    void trim(std::string &str);
    void trimHead(std::string &str);
    void trimTail(std::string &str);
    void strAppendInt(std::string &s, int n);
}